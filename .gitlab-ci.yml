# ---------------------------------------
# Global
# ---------------------------------------

stages:
  - "Build"
  - "Deploy"

# ----------------------------------------
# Build
# ----------------------------------------

build:
  stage: "Build"
  image: alpine:latest
  script:
    - sh build/build.sh
  only:
    - web
    - master

# ----------------------------------------
# Deploy
# ----------------------------------------

deploy:
  stage: "Deploy"
  image: alpine:latest
  script:
    - sh build/package.sh
  artifacts:
    expire_in: 4 weeks
    paths:
      - out/*
  only:
    - web
    - master